import React,{useEffect,useMemo,useState} from 'react';
import{createRoot}from'react-dom/client';
import{CalendarDays,Check,ChevronDown,Copy,ExternalLink,MapPin,Search,Share2,Sparkles,UserRound,X}from'lucide-react';
import{concerts,venues,venueById}from'./data';import'./styles.css';
const DA=['SØN','MAN','TIR','ONS','TOR','FRE','LØR'],MON=['JAN','FEB','MAR','APR','MAJ','JUN','JUL','AUG','SEP','OKT','NOV','DEC'];
const fmt=d=>{let x=new Date(d+'T12:00:00');return `${DA[x.getDay()]} ${String(x.getDate()).padStart(2,'0')}.${MON[x.getMonth()]}`};
const useLocal=(key,init)=>{const[s,set]=useState(()=>{try{return JSON.parse(localStorage.getItem(key))??init}catch{return init}});useEffect(()=>localStorage.setItem(key,JSON.stringify(s)),[key,s]);return[s,set]};
function Header({page,setPage,user,onLogin}){return <><div className="marquee"><span>SEND LINKET ✶ FÅ SELSKAB ✶ +1 ✶ KØBENHAVN & OMEGN ✶ SEND LINKET ✶ FÅ SELSKAB ✶ +1 ✶ KØBENHAVN & OMEGN ✶ </span><span aria-hidden>SEND LINKET ✶ FÅ SELSKAB ✶ +1 ✶ KØBENHAVN & OMEGN ✶ SEND LINKET ✶ FÅ SELSKAB ✶ +1 ✶ KØBENHAVN & OMEGN ✶ </span></div><header><button className="logo" onClick={()=>setPage('home')}>+1</button><nav><button className={page==='home'?'active':''} onClick={()=>setPage('home')}>Koncerter</button><button className={page==='venues'?'active':''} onClick={()=>setPage('venues')}>Spillesteder</button><button className={page==='profile'?'active':''} onClick={()=>user?setPage('profile'):onLogin()}>Min liste</button></nav><button className="login" onClick={()=>user?setPage('profile'):onLogin()}>{user?<><span className="tinyavatar">{user.name[0]}</span>{user.name}</>:'Log ind'}</button></header></>}
function ShareCard({user,onLogin,rsvps}){let going=Object.values(rsvps).filter(x=>x==='going').length,maybe=Object.values(rsvps).filter(x=>x==='maybe').length;const copy=()=>navigator.clipboard?.writeText(`${location.origin}/?u=${user?.username||'din-liste'}`);return <div className="share-wrap"><i className="tape"/><div className="share-card"><div className="identity"><div className="avatar">{user?.name?.[0]||'J'}</div><div><b>{user?`${user.name}s liste`:'Din koncertliste'}</b><small>plusen.dk/{user?.username||'dit-navn'}</small></div></div><p>Skal til <strong>{going} koncerter</strong> · overvejer <strong>{maybe}</strong><br/>Ét link. Hele din koncertkalender.</p><button onClick={user?copy:onLogin}>{user?'Kopiér dit link':'Lav din liste'} <Copy size={15}/></button></div><div className="star"><i/><i/><span>DEL<br/>MIG!</span></div></div>}
function RsvpButtons({id,value,onChange}){return <div className="actions"><button className={'going '+(value==='going'?'selected':'')} onClick={()=>onChange(id,value==='going'?null:'going')}>{value==='going'?<><Check/> Du kommer!</>:<>+1 Jeg kommer!</>}</button><button className={'maybe '+(value==='maybe'?'selected':'')} onClick={()=>onChange(id,value==='maybe'?null:'maybe')}>{value==='maybe'?<>Måske <Check/></>:<>Måske…</>}</button></div>}
function ConcertList({items,rsvps,onRsvp,empty='Ingen koncerter her endnu.'}){if(!items.length)return <div className="empty">{empty}<Sparkles/></div>;return <div className="concerts">{items.map(c=>{let v=venueById(c.venueId);return <article className="concert" key={c.id}><time>{fmt(c.date)}</time><div className="concert-main"><button className="artist" onClick={()=>window.open(c.url,'_blank')}>{c.artist}</button><div className="meta"><button onClick={()=>location.hash=`venue:${v.id}`}>{v.name}</button> · {c.genre}{c.price?` · ${c.price} kr`:''}</div></div><RsvpButtons id={c.id} value={rsvps[c.id]} onChange={onRsvp}/></article>})}</div>}
function LoginModal({close,login,pending}){const[name,setName]=useState('');return <div className="modal-bg" onMouseDown={e=>e.target===e.currentTarget&&close()}><form className="modal" onSubmit={e=>{e.preventDefault();if(name.trim())login(name.trim())}}><button type="button" className="close" onClick={close}><X/></button><span className="eyebrow">{pending?'Gem dit svar':'Din liste venter'}</span><h2>Hvem skal<br/><em>med?</em></h2><p>Vælg et navn, så gemmer vi dine koncerter på denne enhed. Ingen mail. Intet bøvl.</p><label>DIT NAVN<input autoFocus value={name} onChange={e=>setName(e.target.value)} placeholder="fx Anna" maxLength="24"/></label><button className="submit">Lav min liste →</button><small>V1-demo: Din liste gemmes lokalt i browseren.</small></form></div>}
function Home({user,onLogin,rsvps,onRsvp}){const[filter,setFilter]=useState('all'),[query,setQuery]=useState(''),[limit,setLimit]=useState(14);const items=useMemo(()=>concerts.filter(c=>(filter==='all'||c.venueId===filter)&&c.artist.toLowerCase().includes(query.toLowerCase())).slice(0,limit),[filter,query,limit]);return <><section className="hero"><div className="hero-copy"><i className="dots"/><span className="eyebrow">DIN NÆSTE STORE AFTEN →</span><h1>Se hvad dine venner <em>skal se.</em></h1><p>Koncerter i København samlet ét sted. Sig <strong>»jeg kommer!«</strong> — eller et blødt måske — og del din liste med ét link.</p><div className="hero-actions"><button onClick={()=>document.querySelector('#feed').scrollIntoView({behavior:'smooth'})}>Find koncerter ↓</button><button onClick={onLogin}>{user?'Se min liste':'Start din liste'} <Share2/></button></div></div><ShareCard user={user} onLogin={onLogin} rsvps={rsvps}/></section><main id="feed"><div className="feed-head"><div><span className="eyebrow">OPDAG</span><h2>Hvad spiller?</h2></div><label className="search"><Search/><input aria-label="Søg efter artist" placeholder="Søg artist…" value={query} onChange={e=>setQuery(e.target.value)}/></label></div><div className="chips"><button className={filter==='all'?'on':''} onClick={()=>setFilter('all')}>Alle</button>{venues.map(v=><button key={v.id} className={filter===v.id?'on':''} onClick={()=>setFilter(v.id)}>{v.name}</button>)}</div><div className="result-count">{concerts.filter(c=>filter==='all'||c.venueId===filter).length} kommende koncerter · opdateret 13. august 2026</div><ConcertList items={items} rsvps={rsvps} onRsvp={onRsvp}/>{items.length<concerts.filter(c=>filter==='all'||c.venueId===filter).length&&<button className="more" onClick={()=>setLimit(x=>x+14)}>Vis flere <ChevronDown/></button>}</main></>}
function Venues({setVenue}){return <main className="subpage"><span className="eyebrow">KBH & OMEGN</span><h1>Spillestederne.</h1><p className="intro">Fra intime kældre til sale med plads til hele vennegruppen.</p><div className="venue-grid">{venues.map((v,i)=><button key={v.id} onClick={()=>setVenue(v)}><span>0{(i+1)}</span><h2>{v.name}</h2><p><MapPin/> {v.area}</p><b>Se kalender →</b></button>)}</div></main>}
function VenuePage({venue,back,rsvps,onRsvp}){return <main className="subpage"><button className="back" onClick={back}>← Alle spillesteder</button><section className="venue-hero"><i className="dots"/><span className="eyebrow">SPILLESTED</span><h1>{venue.name}</h1><p><MapPin/> {venue.address} · {venue.area}</p><a href={venue.url} target="_blank">Officiel kalender <ExternalLink/></a></section><div className="section-title"><h2>Kommende koncerter</h2><span>{concerts.filter(c=>c.venueId===venue.id).length} fundet</span></div><ConcertList items={concerts.filter(c=>c.venueId===venue.id)} rsvps={rsvps} onRsvp={onRsvp}/></main>}
function Profile({user,rsvps,onRsvp}){let go=concerts.filter(c=>rsvps[c.id]==='going'),maybe=concerts.filter(c=>rsvps[c.id]==='maybe');const copy=()=>navigator.clipboard?.writeText(location.href);return <main className="subpage profile"><div className="profile-hero"><div className="big-avatar">{user.name[0]}</div><div><span className="eyebrow">OFFENTLIG LISTE</span><h1>{user.name}s koncerter.</h1><p>plusen.dk/{user.username}</p></div><button onClick={copy}>Kopiér link <Copy/></button></div><div className="section-title pink"><h2>Jeg kommer!</h2><span>{go.length}</span></div><ConcertList items={go} rsvps={rsvps} onRsvp={onRsvp} empty="Du har ikke sagt ja endnu. Det kan nås!"/><div className="section-title yellow"><h2>Jeg overvejer…</h2><span>{maybe.length}</span></div><ConcertList items={maybe} rsvps={rsvps} onRsvp={onRsvp} empty="Ingen løse planer — endnu."/></main>}
function App(){const[user,setUser]=useLocal('plus-user',null),[rsvps,setRsvps]=useLocal('plus-rsvps',{}),[page,setPage]=useState('home'),[venue,setVenue]=useState(null),[modal,setModal]=useState(false),[pending,setPending]=useState(null);const request=(id,type)=>{if(!user){setPending({id,type});setModal(true)}else setRsvps(x=>({...x,[id]:type}))};const login=name=>{let username=name.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,'-');setUser({name,username});if(pending)setRsvps(x=>({...x,[pending.id]:pending.type}));setPending(null);setModal(false)};useEffect(()=>{const f=()=>{if(location.hash.startsWith('#venue:')){let v=venueById(location.hash.slice(7));if(v){setVenue(v);setPage('venue')}}};addEventListener('hashchange',f);return()=>removeEventListener('hashchange',f)},[]);return <div className="site"><Header page={page} setPage={p=>{setPage(p);setVenue(null);scrollTo(0,0)}} user={user} onLogin={()=>setModal(true)}/>{page==='home'&&<Home user={user} onLogin={()=>user?setPage('profile'):setModal(true)} rsvps={rsvps} onRsvp={request}/>} {page==='venues'&&<Venues setVenue={v=>{setVenue(v);setPage('venue');scrollTo(0,0)}}/>}{page==='venue'&&venue&&<VenuePage venue={venue} back={()=>setPage('venues')} rsvps={rsvps} onRsvp={request}/>} {page==='profile'&&user&&<Profile user={user} rsvps={rsvps} onRsvp={request}/>}<footer><b>+1</b><span>koncerter i KBH & omegn</span><span>gratis at sige ja ✶</span></footer>{modal&&<LoginModal close={()=>{setModal(false);setPending(null)}} login={login} pending={pending}/>}</div>}
createRoot(document.getElementById('root')).render(<App/>);
